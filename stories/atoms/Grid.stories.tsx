import type { Meta, StoryObj } from '@storybook/react'
import { Grid } from '@hce/design-system'

const meta: Meta<typeof Grid> = {
  title: 'Atoms/Grid',
  component: Grid,
  tags: ['autodocs'],
}
export default meta

type Story = StoryObj<typeof Grid>

export const Default: Story = {

}
