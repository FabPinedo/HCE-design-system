import type { Meta, StoryObj } from '@storybook/react'
import { Overlay } from '@hce/design-system'

const meta: Meta<typeof Overlay> = {
  title: 'Atoms/Overlay',
  component: Overlay,
  tags: ['autodocs'],
}
export default meta

type Story = StoryObj<typeof Overlay>

export const Default: Story = {

}
