import type { Meta, StoryObj } from '@storybook/react'
import { MenuItem } from '@hce/design-system'

const meta: Meta<typeof MenuItem> = {
  title: 'Atoms/MenuItem',
  component: MenuItem,
  tags: ['autodocs'],
}
export default meta

type Story = StoryObj<typeof MenuItem>

export const Default: Story = {

}
